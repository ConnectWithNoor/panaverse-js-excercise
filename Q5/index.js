const quote =
  'Kindness is one thing you can’t give away. It always comes back.';
const famous_person = 'George Skolsky';

const message = `${famous_person} once said, “${quote}”`;

console.log(message);
